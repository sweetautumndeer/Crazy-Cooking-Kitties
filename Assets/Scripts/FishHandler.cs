/*
Music Manager
Autumn Moulios
Last Updated 07/24/2021

Keeps a list of upcoming fish, and displays them
in the scene
*/

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FishHandler : MonoBehaviour {
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void ParseFishType(string markerName) {
        switch (markerName) {
            case "Long":

                break;
            case "Medium":

                break;
            case "Small":

                break;
        }
    }
}
